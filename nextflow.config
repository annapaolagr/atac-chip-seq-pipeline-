aws {
    client {
        maxConnections = 20
        connectionTimeout = 1000000
    }
}

params {
    // 1. Cambiato da wildcard a file samplesheet
    input            = "samplesheet.csv" 
    
    // 2. Aggiunto parametro per decidere il tipo di analisi
    protocol         = "chip" // Default: chip. Opzioni: 'chip' o 'atac'
    
    outdir           = "results"
    genome           = 'hg38'
    igenomes_base    = 's3://ngi-igenomes/igenomes/'
    bowtie2_index    = null 
    publish_dir_mode = 'copy'
}

process.shell = ['/bin/bash', '-euo', 'pipefail']
includeConfig 'conf/igenomes.config'

profiles {
    docker {
        docker.enabled         = true
        docker.runOptions      = '-u $(id -u):$(id -g)'
    }
    singularity {
        singularity.enabled    = true
        singularity.autoMounts = true
        singularity.registry   = 'quay.io'
    }
}

executor {
    queueSize = 4 
}

process {
    cpus   = 2
    memory = '4 GB'

    withName: FASTQC {
        cpus   = 2
        memory = '4 GB'
        publishDir = [ path: "${params.outdir}/01_fastqc", mode: params.publish_dir_mode ]
    }

    withName: TRIMGALORE {
        cpus   = 4
        memory = '8 GB'
        publishDir = [ path: "${params.outdir}/02_trimmed", mode: params.publish_dir_mode ]
    }

    withName: BOWTIE2 {
        cpus   = 12
        memory = '32 GB'
        // ext.args flessibile per protocollo (gestibile anche nel modulo)
        ext.args   = '--very-sensitive -X 2000 --no-mixed --no-discordant'
        publishDir = [ path: "${params.outdir}/03_aligned_raw", mode: params.publish_dir_mode, enabled: false ]
    }

    withName: SAMTOOLS_SORT {
        cpus   = 6
        memory = '20 GB'
        ext.args   = '-m 800M' 
        publishDir = [ path: "${params.outdir}/03_aligned_sorted", mode: params.publish_dir_mode ]
    }

    withName: PICARD_MARKDUPLICATES {
        cpus   = 2
        memory = '24 GB'
        ext.args   = '--REMOVE_DUPLICATES true --ASSUME_SORTED true --VALIDATION_STRINGENCY LENIENT --CREATE_INDEX true'
        publishDir = [ path: "${params.outdir}/04_deduplicated", mode: params.publish_dir_mode ]
    }

    // 3. Risorse per MACS3 ATAC (Senza Controllo)
    withName: MACS3_ATAC {
        cpus   = 4
        memory = '16 GB'
        publishDir = [ path: "${params.outdir}/05_peaks/atac", mode: params.publish_dir_mode ]
    }

    // 4. Risorse per MACS3 CHIP (Con Controllo IP vs Input)
    withName: MACS3_CHIP {
        cpus   = 4
        memory = '16 GB'
        publishDir = [ path: "${params.outdir}/05_peaks/chip", mode: params.publish_dir_mode ]
    }
}
